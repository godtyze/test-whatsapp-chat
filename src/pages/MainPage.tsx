import { FC } from 'react';

import { Layout } from 'antd';

const MainPage: FC = () => {
  return (
    <Layout>
      <Layout.Content></Layout.Content>
    </Layout>
  );
};

export default MainPage;
